apiVersion: v1
kind: Service
metadata:
  name: confluence-mcp-server
  namespace: rhdh
  labels:
    app: confluence-mcp-server
    app.kubernetes.io/name: confluence-mcp
spec:
  selector:
    app: confluence-mcp-server
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  type: ClusterIP
